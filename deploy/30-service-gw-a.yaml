apiVersion: v1
kind: Service
metadata:
  name: nvmeof-gw-a
  namespace: nvmeof-gateway
  labels:
    app: nvmeof
    gateway: gw-a
  annotations:
    metallb.universe.tf/loadBalancerIPs: "10.144.27.224"
spec:
  type: LoadBalancer
  selector:
    app: nvmeof
    gateway: gw-a
  ports:
    - { name: grpc,      port: 5500, targetPort: 5500, protocol: TCP }
    - { name: nvme-tcp,  port: 4420, targetPort: 4420, protocol: TCP }
    - { name: discovery, port: 8009, targetPort: 8009, protocol: TCP }
